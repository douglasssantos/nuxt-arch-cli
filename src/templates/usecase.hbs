export interface I{{pascalName}}UseCase<TInput = unknown, TOutput = unknown> {
  execute(input: TInput): Promise<TOutput>
}

export class {{pascalName}}UseCase implements I{{pascalName}}UseCase {
  constructor(
    // TODO: inject repositories and services
  ) {}

  async execute(input: unknown): Promise<unknown> {
    // TODO: implement use case logic
    return input
  }
}
