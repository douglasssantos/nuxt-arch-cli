import type { {{pascalName}} } from '../../domain/entities/{{pascalName}}'

export interface {{pascalName}}Dto {
  id: string | number | null
  createdAt: string | null
  updatedAt: string | null
}

export class {{pascalName}}Mapper {
  static toDomain(dto: {{pascalName}}Dto): {{pascalName}} {
    return {
      id: dto.id,
      createdAt: dto.createdAt,
      updatedAt: dto.updatedAt,
    } as {{pascalName}}
  }

  static toDto(domain: {{pascalName}}): {{pascalName}}Dto {
    return {
      id: domain.id,
      createdAt: domain.createdAt,
      updatedAt: domain.updatedAt,
    }
  }

  static toDomainList(dtos: {{pascalName}}Dto[]): {{pascalName}}[] {
    return dtos.map((dto) => this.toDomain(dto))
  }
}
