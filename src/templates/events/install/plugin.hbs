import { EventBus } from '{{eventsImportBase}}/services/EventBus'
import { registerEvents } from '{{eventsImportBase}}/EventRegistry'

export default defineNuxtPlugin(() => {
  const eventBus = new EventBus()
  registerEvents(eventBus)

  return {
    provide: {
      eventBus,
    },
  }
})
