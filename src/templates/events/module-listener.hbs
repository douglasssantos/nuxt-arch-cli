import type { IEventHandler } from '~/core/events/contracts/IEventHandler'
import type { {{eventPascal}}Payload } from '../events/{{eventPascal}}'

/**
 * {{pascalName}} Listener — Module: {{layer}}
 * Handles: {{eventKey}}
 *
 * @generated Nuxt CLI
 */
export class {{pascalName}} implements IEventHandler<{{eventPascal}}Payload> {
  async handle(payload: {{eventPascal}}Payload): Promise<void> {
    // TODO: implement listener logic
    void payload
  }
}
