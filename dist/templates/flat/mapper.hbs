import type { {{pascalName}} } from '../models/{{pascalName}}'

export interface {{pascalName}}ApiResponse {
  id: string | number | null
  created_at?: string | null
  updated_at?: string | null
  [key: string]: unknown
}

export class {{pascalName}}Mapper {
  static toModel(response: {{pascalName}}ApiResponse): {{pascalName}} {
    return {
      id: response.id,
      createdAt: response.created_at ?? null,
      updatedAt: response.updated_at ?? null,
    } as {{pascalName}}
  }

  static toApi(model: Partial<{{pascalName}}>): Omit<{{pascalName}}ApiResponse, 'id'> {
    return {
      created_at: (model as {{pascalName}}).createdAt ?? null,
      updated_at: (model as {{pascalName}}).updatedAt ?? null,
    }
  }

  static toModelList(responses: {{pascalName}}ApiResponse[]): {{pascalName}}[] {
    return responses.map((r) => this.toModel(r))
  }
}
