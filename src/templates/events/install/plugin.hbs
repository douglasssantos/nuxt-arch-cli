import { EventBus } from '~/core/events/services/EventBus'
import { registerEvents } from '~/core/events/EventRegistry'

export default defineNuxtPlugin(() => {
  const eventBus = new EventBus()
  registerEvents(eventBus)

  return {
    provide: {
      eventBus,
    },
  }
})
