import { useRepo } from 'pinia-orm'
import type { I{{pascalName}}Repository } from '../../domain/repositories/I{{pascalName}}Repository'
import type { {{pascalName}} } from '../../domain/entities/{{pascalName}}'
import { {{pascalName}} as {{pascalName}}Model } from '../stores/{{pascalName}}Store'

export class {{pascalName}}Repository implements I{{pascalName}}Repository {
  private get repo() {
    return useRepo({{pascalName}}Model)
  }

  async findById(id: string | number): Promise<{{pascalName}} | null> {
    return this.repo.find(id) as {{pascalName}} | null
  }

  async findAll(): Promise<{{pascalName}}[]> {
    return this.repo.all() as {{pascalName}}[]
  }

  async save(entity: {{pascalName}}): Promise<{{pascalName}}> {
    return this.repo.save(entity) as {{pascalName}}
  }

  async delete(id: string | number): Promise<void> {
    this.repo.destroy(id)
  }
}
