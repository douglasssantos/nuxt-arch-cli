export default defineNuxtPlugin((nuxtApp) => {
  // TODO: initialize {{pascalName}} plugin
  // nuxtApp.provide('{{camelName}}', ...)

  return {
    provide: {
      {{camelName}}: {} as Record<string, unknown>,
    },
  }
})
